---
import Base from '../../../layouts/Base.astro';
import SiteHeader from '../../../components/SiteHeader.astro';
import SiteFooter from '../../../components/SiteFooter.astro';
import { getCollection } from 'astro:content';

const POSTS_PER_PAGE = 12;
const url = Astro.url;
const pageParam = url.searchParams.get('page');
const currentPage = pageParam ? Math.max(1, parseInt(pageParam, 10)) : 1;

const allPosts = (await getCollection('blog', ({ data }) => {
  return data.locale === 'ms' && !data.draft;
})).sort((a, b) => b.data.publishedAt.getTime() - a.data.publishedAt.getTime());

const totalPages = Math.ceil(allPosts.length / POSTS_PER_PAGE);
const posts = allPosts.slice((currentPage - 1) * POSTS_PER_PAGE, currentPage * POSTS_PER_PAGE);

function formatDate(date: Date): string {
  return date.toLocaleDateString('ms-MY', { day: 'numeric', month: 'short', year: 'numeric' });
}

function getSlug(entry: (typeof allPosts)[0]): string {
  return entry.slug;
}
---

<Base
  title="Panduan Keselamatan Siber untuk PKS Malaysia | Cyber Secure SME"
  description="Panduan keselamatan siber dalam bahasa yang mudah difahami untuk pemilik perniagaan Malaysia: pematuhan PDPA, pertahanan ransomware, pencegahan pancingan data, pelesenan NACSA, dan memilih pembekal keselamatan yang tepat."
  locale="ms"
  altLocaleHref="/blog"
>
  <SiteHeader locale="ms" currentPath="/ms/blog" altLocaleHref="/blog" />

  <main>
    <!-- Page header -->
    <section style="padding:72px 0 48px 0;border-bottom:1px solid #181818;">
      <div class="container">
        <p class="eyebrow" style="margin:0 0 16px 0;">PANDUAN KESELAMATAN SIBER</p>
        <h1 class="heading" style="font-size:clamp(28px,5vw,48px);margin:0 0 18px 0;line-height:1.05;max-width:700px;">
          Panduan keselamatan siber untuk PKS Malaysia
        </h1>
        <p style="font-family:'EB Garamond',Garamond,Georgia,serif;font-size:17px;line-height:1.65;color:#181818;margin:0;max-width:600px;">
          Artikel dalam bahasa yang mudah difahami untuk membantu perniagaan Malaysia memahami risiko siber,
          memenuhi obligasi PDPA dan NACSA, serta memilih pembekal keselamatan yang tepat.
        </p>
      </div>
    </section>

    <!-- Trust strip -->
    <div style="border-bottom:1px solid #d6d6cf;padding:14px 0;">
      <div class="container" style="display:flex;align-items:center;gap:24px;flex-wrap:wrap;">
        <span class="eyebrow" style="flex-shrink:0;color:#6e6e6e;">RUJUKAN</span>
        {['NACSA (Akta 854)', 'CyberSecurity Malaysia', 'PDPA / JPDP (Akta 709)', 'MCMC', 'BNM RMiT', 'ISO/IEC 27001'].map(reg => (
          <span class="eyebrow" style="color:#6e6e6e;display:flex;align-items:center;gap:5px;text-transform:none;letter-spacing:0.03em;font-size:11px;">
            <span style="width:5px;height:5px;border:1px solid #181818;flex-shrink:0;"></span>
            {reg}
          </span>
        ))}
      </div>
    </div>

    <!-- Article list -->
    <section>
      <div class="container" style="padding-top:0;padding-bottom:0;">
        {posts.length === 0 && (
          <p style="padding:64px 0;color:#6e6e6e;font-family:'EB Garamond',Garamond,Georgia,serif;font-size:17px;">Tiada artikel diterbitkan lagi. Semak semula tidak lama lagi.</p>
        )}
        {posts.map((post, i) => (
          <article style={`padding:36px 0;border-bottom:1px solid #d6d6cf;${i === 0 ? 'padding-top:44px;' : ''}`}>
            <div style="display:flex;align-items:flex-start;gap:32px;flex-wrap:wrap;">
              <div style="flex:1;min-width:260px;">

                <!-- Meta row -->
                <div style="display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin-bottom:12px;">
                  <time
                    datetime={post.data.publishedAt.toISOString()}
                    class="eyebrow"
                    style="color:#6e6e6e;"
                  >
                    {formatDate(post.data.publishedAt)}
                  </time>
                  {post.data.categories.length > 0 && (
                    <span class="eyebrow" style="color:#181818;background:#ececec;padding:2px 8px;">
                      {post.data.categories[0].replace(/-/g,' ')}
                    </span>
                  )}
                  {post.data.readingTime && (
                    <span class="eyebrow" style="color:#6e6e6e;">
                      {post.data.readingTime} MIN BACA
                    </span>
                  )}
                </div>

                <!-- Title -->
                <h2 class="heading" style="font-size:clamp(20px,2.5vw,26px);margin:0 0 10px 0;line-height:1.2;">
                  <a
                    href={`/ms/blog/${getSlug(post)}`}
                    class="post-title-link"
                  >
                    {post.data.title}
                  </a>
                </h2>

                <!-- Description -->
                <p style="font-family:'EB Garamond',Garamond,Georgia,serif;font-size:16px;line-height:1.62;color:#181818;margin:0 0 16px 0;max-width:680px;">
                  {post.data.description}
                </p>

                <!-- Arrow link -->
                <a href={`/ms/blog/${getSlug(post)}`} class="arrow-link" style="font-size:17px;">
                  Baca artikel
                </a>
              </div>
            </div>
          </article>
        ))}
      </div>
    </section>

    <!-- Pagination -->
    {totalPages > 1 && (
      <nav style="padding:48px 0;" aria-label="Navigasi blog">
        <div class="container" style="display:flex;justify-content:space-between;align-items:center;">
          {currentPage > 1 ? (
            <a
              href={`/ms/blog${currentPage - 1 > 1 ? `?page=${currentPage - 1}` : ''}`}
              class="btn-secondary"
              style="font-size:16px;"
            >
              &larr; Artikel terbaru
            </a>
          ) : <span />}
          <span class="eyebrow" style="color:#6e6e6e;">
            HALAMAN {currentPage} DARIPADA {totalPages}
          </span>
          {currentPage < totalPages ? (
            <a
              href={`/ms/blog?page=${currentPage + 1}`}
              class="btn-secondary"
              style="font-size:16px;"
            >
              Artikel lama &rarr;
            </a>
          ) : <span />}
        </div>
      </nav>
    )}
  </main>

  <SiteFooter locale="ms" currentPath="/ms/blog" altLocaleHref="/blog" />
</Base>

<style>
  .post-title-link {
    color: #181818;
    text-decoration: none;
    transition: color 100ms;
  }
  .post-title-link:hover {
    color: #181818;
    text-decoration: underline;
    text-decoration-thickness: 1px;
    text-underline-offset: 3px;
  }
</style>
